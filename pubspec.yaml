name: chess_app
description: A complete Chess game for Android and iOS.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  google_mobile_ads: ^4.0.0
  in_app_purchase: ^3.2.0

flutter:
  uses-material-design: true
